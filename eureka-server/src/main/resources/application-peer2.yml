spring:
  application:
    name: eureka-server

server:
  port: 10002

eureka:
  instance:
    hostname: peer2
  client:
    serviceUrl:
      defaultZone: http://peer1:10001/eureka/,http://peer3:10003/eureka/
